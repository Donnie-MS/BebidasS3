import tito.*
import bebidas wlk.*
describe "Comportamiento de Tito" {
  test "Tito pesa 70 kilos" {
    assert.equals(tito.peso(), 70)
  }
  test "La inercia de Tito es de 490" {
    assert.equals(tito.inercia(), 490)  
  }
}
